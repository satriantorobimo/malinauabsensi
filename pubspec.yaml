name: malinau_absensi
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+13

environment:
  sdk: ^3.5.0

dependencies:
  cupertino_icons: ^1.0.8
  dropdown_button2: ^2.3.9
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  provider: ^6.1.2
  camera: ^0.10.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.2
  flutter_bloc: ^8.1.1
  http: ^1.0.0
  equatable: ^2.0.5
  google_mlkit_face_detection: ^0.12.0
  geolocator: ^13.0.1
  intl: ^0.19.0
  google_ml_vision: ^0.0.8
  image: ^4.3.0
  url_launcher: ^6.3.1
  web_socket_channel: ^2.1.0
  json_annotation: ^4.8.1
  auto_size_text: ^3.0.0
  community_charts_flutter: ^1.0.4
  shimmer: ^3.0.0
  image_picker: ^1.1.2
  file_picker: ^8.1.4

dev_dependencies:
  change_app_package_name: ^1.3.0
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/imgs/logo.png"
  adaptive_icon_foreground: "assets/imgs/logo.png" # Set the foreground image (same or different from image_path)
  adaptive_icon_background: "#423F91"

flutter:
  uses-material-design: true

  assets:
    - assets/imgs/
    - assets/icons/
