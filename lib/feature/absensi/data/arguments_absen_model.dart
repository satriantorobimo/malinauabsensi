import 'package:camera_camera/camera_camera.dart';

class ArgumentAbsenModel {
  final CameraDescription camera;
  final bool isIn;

  ArgumentAbsenModel({required this.camera, required this.isIn});
}
